{
  "name": "credibility-viewer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Vite + React viewer for the credibility_check report. In dev, uses Vite's built-in proxy to forward /api calls to Railway with the RENDER_TOKEN attached server-side. In production (Railway), the Express server in server.js does the same job — serves the static dist/ build and proxies /api with the token, so the token never touches the browser bundle.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "http-proxy-middleware": "^3.0.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
